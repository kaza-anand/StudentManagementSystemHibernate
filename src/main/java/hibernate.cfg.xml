<hibernate-configuration>

    <session-factory>

        <!-- Database Connection Settings -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/StudentDB</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">12345678</property>

        <!-- Hibernate Settings -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- To display the generated SQL in console -->
        <property name="hibernate.show_sql">true</property>

        <!-- To format SQL output in readable form -->
        <property name="hibernate.format_sql">true</property>

        
        <mapping class="com.student.StudentManagementSystemHibernate.Student" />
        <mapping class="com.student.StudentManagementSystemHibernate.Marks" />


    </session-factory>

</hibernate-configuration>